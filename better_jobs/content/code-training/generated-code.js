window.BJ_CODE_SNIPPETS = {
  "binary-search/search-rotated-array": "﻿def search_rotated_array(nums: list[int], target: int) -> int:\n    \"\"\"\n    旋转有序数组搜索：在没有重复元素的前提下，用二分保持 O(log n)。\n\n    核心判断：每一轮至少有一半区间是严格有序的。\n    - 如果 nums[left] <= nums[mid]，说明左半边有序。\n    - 否则右半边有序。\n    只要 target 落在有序半边，就收缩到那一半；否则去另一半。\n\n    时间复杂度：O(log n)\n    空间复杂度：O(1)\n    \"\"\"\n    left, right = 0, len(nums) - 1\n\n    while left <= right:\n        mid = (left + right) // 2\n        if nums[mid] == target:\n            return mid\n\n        # 左半区间 [left, mid] 有序：可以直接判断 target 是否落在这段单调区间里。\n        if nums[left] <= nums[mid]:\n            if nums[left] <= target < nums[mid]:\n                right = mid - 1\n            else:\n                left = mid + 1\n        # 否则右半区间 [mid, right] 有序，同理判断 target 是否落在右半边。\n        else:\n            if nums[mid] < target <= nums[right]:\n                left = mid + 1\n            else:\n                right = mid - 1\n\n    return -1\r\n",
  "design/lru-cache": "﻿class Node:\n    \"\"\"双向链表节点：同时保存 key 和 value，方便淘汰尾节点时从哈希表删除。\"\"\"\n\n    def __init__(self, key: int = 0, value: int = 0):\n        self.key = key\n        self.value = value\n        self.prev: Node | None = None\n        self.next: Node | None = None\n\n\nclass LRUCache:\n    \"\"\"\n    LRU 缓存手撕模板：哈希表 + 双向链表。\n\n    不变量：\n    - head 后面的节点永远是最近使用的节点。\n    - tail 前面的节点永远是最久未使用的节点。\n    - cache[key] 直接定位到链表节点，因此 get/put 都是 O(1)。\n\n    复杂度：get O(1)，put O(1)，额外空间 O(capacity)。\n    \"\"\"\n\n    def __init__(self, capacity: int):\n        self.capacity = capacity\n        self.cache: dict[int, Node] = {}\n\n        # 使用哨兵头尾节点，可以避免插入/删除时反复判断空链表或边界节点。\n        self.head = Node()\n        self.tail = Node()\n        self.head.next = self.tail\n        self.tail.prev = self.head\n\n    def get(self, key: int) -> int:\n        if key not in self.cache:\n            return -1\n\n        node = self.cache[key]\n        # 只要被访问，就变成“最近使用”，移动到头部。\n        self._move_to_front(node)\n        return node.value\n\n    def put(self, key: int, value: int) -> None:\n        if key in self.cache:\n            node = self.cache[key]\n            node.value = value\n            self._move_to_front(node)\n            return\n\n        # 新节点一定是最近使用的节点，插到 head 后面。\n        node = Node(key, value)\n        self.cache[key] = node\n        self._add_after_head(node)\n\n        # 超出容量时，淘汰 tail 前面的最久未使用节点。\n        if len(self.cache) > self.capacity:\n            removed = self._remove_tail()\n            del self.cache[removed.key]\n\n    def _move_to_front(self, node: Node) -> None:\n        self._remove(node)\n        self._add_after_head(node)\n\n    def _add_after_head(self, node: Node) -> None:\n        first = self.head.next\n        node.prev = self.head\n        node.next = first\n        first.prev = node\n        self.head.next = node\n\n    def _remove(self, node: Node) -> None:\n        node.prev.next = node.next\n        node.next.prev = node.prev\n\n    def _remove_tail(self) -> Node:\n        node = self.tail.prev\n        self._remove(node)\n        return node\r\n",
  "dp/longest-increasing-subsequence": "﻿def length_of_lis(nums: list[int]) -> int:\n    \"\"\"\n    最长递增子序列长度：贪心 + 二分，也叫 patience sorting 模板。\n\n    tails[i] 表示“长度为 i + 1 的递增子序列中，最小可能结尾值”。\n    tails 不是原数组里的真实子序列，但它的长度一定等于当前 LIS 长度。\n\n    为什么可以替换：同样长度下结尾越小，后续越容易接上更大的数。\n    时间复杂度：O(n log n)，空间复杂度：O(n)。\n    \"\"\"\n    tails: list[int] = []\n\n    for num in nums:\n        left, right = 0, len(tails)\n\n        # 找到第一个 >= num 的位置：num 可以成为该长度下更小的结尾。\n        while left < right:\n            mid = (left + right) // 2\n            if tails[mid] < num:\n                left = mid + 1\n            else:\n                right = mid\n\n        if left == len(tails):\n            # num 比所有结尾都大，可以扩展出更长的递增子序列。\n            tails.append(num)\n        else:\n            # 用更小的结尾替换，保持未来扩展空间最大。\n            tails[left] = num\n\n    return len(tails)\r\n",
  "graph/number-of-islands": "﻿def num_islands(grid: list[list[str]]) -> int:\n    \"\"\"\n    岛屿数量：网格 DFS flood fill 模板。\n\n    思路：遇到一个未访问的陆地 '1'，岛屿数量 +1，然后把与它连通的所有陆地都淹掉。\n    访问标记：直接把 '1' 改成 '0'，避免额外 visited 数组。\n\n    时间复杂度：O(mn)，每个格子最多访问一次。\n    空间复杂度：O(mn)，最坏情况下递归栈可能覆盖整张网格。\n    \"\"\"\n    if not grid or not grid[0]:\n        return 0\n\n    rows, cols = len(grid), len(grid[0])\n    count = 0\n\n    def dfs(row: int, col: int) -> None:\n        # 越界或遇到水域/已访问陆地，直接返回。\n        if row < 0 or row >= rows or col < 0 or col >= cols:\n            return\n        if grid[row][col] != \"1\":\n            return\n\n        # 标记当前陆地已访问，再向四个方向扩散。\n        grid[row][col] = \"0\"\n        dfs(row + 1, col)\n        dfs(row - 1, col)\n        dfs(row, col + 1)\n        dfs(row, col - 1)\n\n    for row in range(rows):\n        for col in range(cols):\n            if grid[row][col] == \"1\":\n                count += 1\n                dfs(row, col)\n\n    return count\r\n",
  "sliding-window/longest-substring": "﻿def length_of_longest_substring(s: str) -> int:\n    \"\"\"\n    无重复字符最长子串：滑动窗口模板。\n\n    不变量：窗口 [left, right] 内始终没有重复字符。\n    last_seen 记录字符上一次出现的位置；如果重复字符落在当前窗口内，就把 left 跳到重复字符后一位。\n\n    时间复杂度：O(n)，每个字符最多被 right 扫一次、left 跳过一次。\n    空间复杂度：O(k)，k 是字符集大小。\n    \"\"\"\n    last_seen: dict[str, int] = {}\n    left = 0\n    best = 0\n\n    for right, char in enumerate(s):\n        # 只有上次出现位置在当前窗口内，才需要移动 left。\n        if char in last_seen and last_seen[char] >= left:\n            left = last_seen[char] + 1\n\n        last_seen[char] = right\n        best = max(best, right - left + 1)\n\n    return best\r\n"
};
